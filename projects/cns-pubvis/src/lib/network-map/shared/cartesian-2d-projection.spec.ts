import { Cartesian2dProjection } from './cartesian-2d-projection';

describe('Cartesian2dProjection', () => {
  it('should create an instance', () => {
    expect(new Cartesian2dProjection(null)).toBeTruthy();
  });
});
